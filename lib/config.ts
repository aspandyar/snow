/**
 * Единственное место в проекте, где живут числа.
 * Каждое значение объясняется: почему именно столько и что сломается иначе.
 */

import type { ПараметрыРаскладки } from '@/lib/layout/bricks'
import type { Дюна, ВершинаКольца, УровеньДробления } from '@/lib/terrain/height'

export const сцена = {
  /** Цвет фона. Светлый намеренно: зритель в конце прокрутки выезжает
   *  из снежной сцены на светлый лендинг, и тёмный фон читался бы как
   *  переход на другой сайт. Здесь же он задаёт тон, под который
   *  подбирается цвет кирпича — кирпич должен быть темнее. */
  фон: '#e8ecef',

  камера: {
    /** Начальное положение камеры, метры. Отодвинута по всем трём осям,
     *  чтобы объект был виден в три четверти, а не в лоб: в лоб не
     *  читается ни объём, ни кладка.
     *
     *  Дальность до центра сферы — 58 метров, почти вдесятеро больше её
     *  радиуса. Это ВДВОЕ дальше прежних 29 метров: с прежней дальности
     *  сфера занимала половину высоты кадра и читалась как предмет на
     *  столе, а не как объект посреди равнины. Смещение от центра сферы
     *  умножено ровно на два по всем осям, поэтому ракурс остался тем же
     *  — сменился только масштаб. Дальше 80 метров дымка начинает есть
     *  контраст кладки, и сфера превращается в серое пятно.
     *
     *  ДОЛЖНО СОВПАДАТЬ с первой точкой траектория.позиции: кривая
     *  открытая, и при нулевом прогрессе камера обязана оказаться ровно
     *  здесь, иначе первый кадр дёрнется. */
    позиция: [32, 6, 48] as [number, number, number],

    /** Поле зрения ПО ВЕРТИКАЛИ, градусы, на опорном отношении сторон.
     *  Сорок пять — компромисс: шире даёт заметное искажение по краям
     *  кадра на широких экранах, у́же лишает сцену ощущения простора. */
    полеЗрения: 45,

    /** Отношение сторон, при котором кадр выглядит задуманно. Все
     *  остальные числа сцены — положение камеры, дальность общего плана,
     *  высота гор — подбирались глазами именно на нём. 16:9 записано
     *  дробью, а не числом 1.78: так видно, откуда оно взялось. */
    опорноеОтношение: 16 / 9,

    /** Предел поля зрения при подгонке под узкое окно, градусы.
     *
     *  У перспективной камеры поле зрения задаётся по вертикали, поэтому
     *  на телефоне горизонтального охвата остаётся вдвое с лишним меньше:
     *  общий план перестаёт читаться простором, а к середине прокрутки
     *  сфера уже не влезает в кадр. Поле зрения расширяется, чтобы охват
     *  вернуть (см. полеЗренияПоКадру).
     *
     *  Но полностью вернуть его нельзя: на 375×812 точный пересчёт даёт
     *  115 градусов, а это рыбий глаз — прямые выгибаются, сфера уезжает
     *  точкой. Семьдесят — граница, за которой искажение начинает
     *  читаться. Остаток разницы лечится только другой траекторией, а не
     *  полем зрения. */
    пределПоляЗрения: 70,
  },

  /** Высота центра сферы над равниной, метры. При радиусе 6 нижняя точка
   *  оказывается в восьми метрах над снегом. Ниже четырёх — сфера
   *  касается дюн и перестаёт читаться парящей. Выше двадцати — теряет
   *  связь с землёй, тень уходит слишком далеко и объект висит сам по
   *  себе. */
  высотаПарения: 14,
}

export const сфера: ПараметрыРаскладки = {
  /** Радиус внешней поверхности кладки, метры. Задаёт масштаб всему
   *  остальному: от него считаются размер кирпича, дальность точечного
   *  света и высота дюн на этапе 2. Менять — значит перенастраивать
   *  сцену целиком. */
  радиус: 6,

  /** Колец по широте. Единственный рычаг плотности кладки: ширина и
   *  высота кирпича следуют за ним сами. Меньше 12 — сфера читается
   *  многогранником. Больше 40 — швы сливаются в шум, а кирпичей
   *  переваливает за тысячу, и пересчёт матриц в кадре начинает
   *  стоить заметного времени. */
  колец: 28,

  /** Коэффициент в формуле round(k · колец · sin θ). Задаёт пропорцию
   *  кирпича: отношение ширины к высоте равно 2/k. Единица даёт кирпич
   *  два к одному, двойка — точный квадрат. Квадрат читается как
   *  мозаика или чешуя, а не как кладка. */
  коэффициент: 1,

  /** Толщина кирпича по нормали, метры. Видна только на отклонённых
   *  кирпичах и в щелях, через которые бьёт внутренний свет. Тоньше —
   *  щели не читаются; толще — сфера выглядит сложенной из брусков. */
  толщина: 0.35,

  /** Шов как доля от размера кирпича. Ноль — кирпичи смыкаются, свет
   *  изнутри наружу не выходит, и весь замысел пропадает. Снизу
   *  ограничен величиной толщина/(2·радиус) = 0.029: ширина считается
   *  на внешней поверхности, а у внутренней грани кирпичи сходятся
   *  теснее и при меньшем шве пересекаются рёбрами. Больше 0.15 —
   *  кладка распадается на россыпь. */
  шов: 0.06,

  /** Сдвиг нечётных колец вдоль кольца, в долях шага. Полшага дают
   *  кладку вразбежку. Ноль выстраивает стыки в одну линию по
   *  меридиану, и сфера читается как глобус со швом. */
  смещениеВразбежку: 0.5,
}

export const материалКирпича = {
  /** Цвет кирпича. Темнее фона намеренно и это важнее всех остальных
   *  настроек вместе взятых: если сделать снег белым, а объект светлым,
   *  объект растворяется в фоне. Светиться должны только рёбра и швы. */
  цвет: '#5a636e',

  /** Шероховатость. Ниже 0.2 кирпич превращается в зеркало и ловит
   *  отражения, которых в сцене нет. Выше 0.7 фаска перестаёт собирать
   *  свет в полосу, и весь смысл скругления теряется. */
  шероховатость: 0.35,

  /** Металличность. Ноль: лёд — диэлектрик. Любое ненулевое значение
   *  убивает рассеянный цвет и делает кирпич грязно-серым. */
  металличность: 0,

  /** Радиус фаски как доля от толщины. Обязательна: на скруглении свет
   *  собирается в яркую полосу, у острого ребра отражение обрывается и
   *  объект читается пластиковым. Строго меньше половины наименьшей
   *  стороны, иначе геометрия вырождается. */
  фаска: 0.25,

  /** Сегментов на скруглении. Меньше 3 — фаска гранёная и полоса света
   *  распадается на пятна. Больше 6 — треугольников вдвое больше без
   *  видимой разницы, а их и так полтысячи копий. */
  гладкостьФаски: 4,

  /** Сила контурного свечения. Встроенное свойство физического материала,
   *  придуманное для ткани: подсвечивает грани, отвернутые от источника.
   *  Ни одной строки шейдера. Ноль — рёбра гаснут и сфера читается
   *  куском камня. */
  контурноеСвечение: 0.6,

  /** Шероховатость контурного свечения. Ниже 0.2 полоса на ребре
   *  становится резкой и читается как обводка, а не как свет. */
  шероховатостьСвечения: 0.4,

  /** Цвет контурного свечения. Светлее самого кирпича, иначе эффекта
   *  не видно вовсе. */
  цветСвечения: '#c8d6e5',
}

export const свет = {
  /** Яркость рассеянного света. Нужен только чтобы теневые грани не
   *  проваливались в чёрное: без него теневая сторона сферы, куда
   *  солнце не достаёт, теряет кладку целиком — она просто не видна.
   *  0.25 для этого мало. Выше 0.5 — сфера теряет объём и читается
   *  плоской наклейкой. */
  рассеянный: 0.4,

  солнце: {
    /** Положение источника, метры. Высота задаёт УГОЛ падения света и
     *  больше ничего: около 29 градусов над горизонтом. Свет должен идти
     *  скользящим, чтобы неровности снега и фаска кирпичей читались
     *  сами; подними солнце в зенит — свет упадёт отвесно, фаска
     *  перестанет собирать полосу вдоль ребра, и всё станет плоским.
     *
     *  Прежде это число выбиралось ещё и по длине тени на снегу. Теней в
     *  сцене больше нет (см. Lights.tsx), и от того требования осталась
     *  только половина — но она же и была главной. */
    положение: [18, 12, 12] as [number, number, number],

    /** Яркость источника. Слабее — рёбра не загораются и объект
     *  остаётся силуэтом, ради которого затевалась вся правка. Сильно
     *  ярче — кирпичи станут светлее будущего снега и растворятся в
     *  фоне: это ошибка, которая портит кадр сильнее всех остальных
     *  вместе взятых. */
    яркость: 1.8,

    /** Цвет источника. Почти нейтральный, лишь с намёком на тепло.
     *  Прежнее значение #fff4e6 было заметно тёплым — я поставил его,
     *  когда снег был однотонным и холодным, и тепло его уравновешивало.
     *  С настоящей картой снега, у которой альбедо нейтрально, тот же
     *  свет увёл всю равнину в песочный: сцена читалась пустыней, а не
     *  зимой. Уходить в синеву тоже нельзя — насыщенная голубизна
     *  читается как игровая графика. Референс почти монохромен. */
    цвет: '#f7f5f2',

  },

  точечный: {
    /** Яркость источника внутри сферы. Свет выходит через зазоры между
     *  кирпичами, поэтому нижние грани оказываются ярче верхних —
     *  снаружи так не бывает, и глаз сразу считывает, что внутри
     *  что-то есть. Это и есть весь замысел освещения. */
    яркость: 40,

    /** Дальность в метрах. Ограничивать ОБЯЗАТЕЛЬНО. Без ограничения
     *  свет на этапе 2 достанет до снега и выбьет землю под сферой в
     *  белое пятно. Держать чуть больше радиуса сферы, чтобы кирпичи
     *  просвечивались, но наружу не выплёскивалось. */
    дальность: 9,

    /** Показатель затухания. Двойка — физически верное обратноквадратичное
     *  затухание. Меньше — свет стелется дальше и снова достаёт до земли. */
    затухание: 2,
  },

  шарик: {
    /** Радиус самосветящегося шарика внутри, метры.
     *
     *  Прежние 1.6 м держались, пока внутрь сферы не залетала камера. С
     *  четырёх с половиной метров шарик такого размера занимает 92%
     *  высоты кадра — прокрутка заканчивалась белой заливкой во весь
     *  экран. При 0.8 это 45%: светящийся шар впереди, вокруг него видна
     *  кладка изнутри.
     *
     *  Свет через щели от размера почти не зависит — его даёт точечный
     *  источник, а не шарик. Проверено снимками на общем и на среднем
     *  плане: при 1.6, 1.0 и 0.7 щели светятся одинаково. Меньше 0.4 —
     *  шарик перестаёт читаться как тело и становится точкой. */
    радиус: 0.8,

    /** Сегментов сферы шарика. Шарик виден только полосками через щели,
     *  и гранёность на нём неразличима — двадцати достаточно. */
    сегменты: 20,

    /** Цвет свечения. Почти обесцвеченный: насыщенная голубизна
     *  читается как игровая графика, референс почти монохромен. */
    цвет: '#dfeaf5',

    /** Яркость свечения в покое. Выше единицы намеренно: тональная
     *  компрессия для шарика выключена, и всё выше единицы уходит в
     *  ореол (см. свечение.порог). Он ничего не освещает, поэтому снег от
     *  него не светлеет — это главный и безопасный рычаг яркости во всей
     *  сцене. */
    яркость: 3,

    /** До какого радиуса шарик раздувается на переходе, метры.
     *
     *  Приближение заканчивается в 12.5 м от центра A, и чтобы закрыть
     *  кадр по высоте при поле зрения 45°, шарику нужен радиус
     *  12.5·tg(22.5°) = 5.2 м. Взято 5.5 — с запасом, но всё ещё меньше
     *  внутреннего радиуса оболочки 5.83 м: шарик наливает A изнутри и не
     *  прорастает сквозь кладку наружу.
     *
     *  Больше 5.83 — кирпичи начнут торчать из светящегося тела, и вместо
     *  «свет съел кладку» получится «шар проткнул стену». */
    радиусРазгоревшийся: 5.5,

    /** Яркость на полном разгорании. Втрое против покоя: ореол
     *  расширяется быстрее самого тела, и кладку вокруг съедает свет, а
     *  не растущая геометрия. Ниже 5 — тело растёт, а свечения вокруг не
     *  прибавляется, и рост читается надуванием шарика, а не разгоранием. */
    яркостьРазгоревшаяся: 9,
  },
}

export const наведение = {
  /** Угловой радиус влияния курсора, радианы, по поверхности сферы.
   *  Доля затронутых кирпичей равна (1 − cos α)/2: при 0.3 в движении
   *  около одиннадцати кирпичей из 506. Меньше 0.15 — шевелятся два-три
   *  и эффект незаметен. Больше 0.6 — за сорок, и сфера теряет форму. */
  угловойРадиус: 0.3,

  /** Максимальное смещение кирпича вдоль нормали, метры. Соразмерно не
   *  толщине кирпича, а его ширине: при 0.3 отклонение на глаз неотличимо
   *  от неподвижной кладки — с расстояния камеры это меньше пяти пикселей.
   *  Проверено на живой сцене. Больше 2 — кирпичи отрываются и висят в
   *  пустоте отдельными плитками, сфера перестаёт читаться целой. */
  максСмещение: 0.9,

  /** Скорость подтягивания к цели, 1/с, в показателе экспоненты. Меньше
   *  3 — кирпичи текут как желе. Больше 15 — движение читается как
   *  рывок, а не как отклик. */
  скорость: 8,

  /** Порог, ниже которого траектория не рисуется, метры. Без него
   *  полтысячи отрезков нулевой длины попадают в <Segments> и съедают
   *  кадр ни на что. */
  порогТраектории: 0.01,
}

export const рельеф = {
  /** Зерно шума. Любое целое; важно, что оно ЕСТЬ. Без него рельеф
   *  пересобирался бы при каждой перезагрузке, кадр переставал бы
   *  повторяться, а тесты стали бы невоспроизводимыми. Менять — значит
   *  получить другой пейзаж той же породы. */
  зерно: 20260818,

  /** Сторона плоскости, метры. Одна плоскость на всю сцену, а не две:
   *  соблазн сделать мелкую площадку под сферой и грубую равнину вокруг
   *  велик, но на стыке двух сеток остаётся ровная граница, которую не
   *  убрать ничем. Меньше 300 — виден край земли. Больше 600 — сетка
   *  той же плотности перестаёт помещаться в бюджет кадра. */
  размер: 400,

  /** Сегментов сетки по стороне. Даёт ячейку около метра и порядка 295
   *  тысяч треугольников — это укладывается в бюджет: главный рычаг здесь
   *  количество пикселей, а не полигонов. Вдвое меньше — дюны становятся
   *  гранёными. Вдвое больше — миллион с лишним треугольников ради
   *  различий, невидимых с высоты камеры. */
  сегментов: 384,

  наст: {
    /** Амплитуда мелкой фактуры, метры. Это снежная корка, а не холмы:
     *  выше полуметра наст начинает спорить с дюнами и рельеф читается
     *  комковатым. Ноль — земля стеклянно ровная и выдаёт подделку. */
    амплитуда: 0.45,

    /** Частота первой октавы, 1/метр. Обратная величина — характерный
     *  размер бугра, здесь около двадцати метров. Выше — фактура
     *  превращается в рябь и пропадает под собственным алиасингом на
     *  сетке в метр. */
    частота: 0.05,

    /** Октав. Каждая следующая вдвое чаще и вдвое слабее. Три дают
     *  различимую фактуру на трёх масштабах. Больше пяти — вклад падает
     *  ниже точности сетки, а время построения растёт линейно. */
    октав: 3,
  },

  /** Крупные формы расставляются вручную: шум красив, но кадром не
   *  управляет — холм не поставить туда, где он нужен по композиции.
   *  Радиус меньше трёх метров при сетке в метр превратит дюну в
   *  угловатый шип: нижняя граница именно здесь. */
  дюны: [
    { x: -48, z: -62, радиус: 46, высота: 5.5 },
    { x: 70, z: -30, радиус: 60, высота: 4 },
    { x: 24, z: 78, радиус: 38, высота: 3 },
    { x: -90, z: 40, радиус: 52, высота: 6.5 },
  ] as Дюна[],
}

export const горы = {
  /** Насколько нижний край кольца опущен ниже нуля, метры. Верхний край
   *  несёт силуэт, нижний нужен лишь затем, чтобы гора не отрывалась от
   *  равнины: без запаса при взгляде камеры чуть сверху между краем снега
   *  и подножием открывается щель, сквозь которую видна пустота за
   *  сценой. Сорок метров перекрывают любой разумный наклон; глубже
   *  смысла нет — снизу всё равно ничего не видно. */
  заглубление: 40,

  /** Сегментов по КРУГУ. Прежние гряды обходились двумя дюжинами на
   *  сектор, но кольцу нужно закрыть все 360°, иначе колокола вершин
   *  огранятся в пилу. Девяносто шесть — это 3.75° на сегмент, самая
   *  узкая вершина ложится на дюжину отрезков и читается гладкой. Больше
   *  трёхсот — плата растёт, а разница тонет в дымке.
   *
   *  Сегментов должно быть вчетверо больше числа самых мелких отрогов
   *  (см. дробление ниже), иначе отрог не помещается в сетку и вместо
   *  ребра выходит случайная зазубрина. При ста двадцати отрогах это
   *  480. Число сегментов и число отрогов связаны, менять их порознь
   *  бессмысленно.
   *
   *  Семьсот отрезков на кольцо — это полторы тысячи треугольников,
   *  вдвое меньше, чем в одном кирпиче сферы. Дёшево. */
  сегментов: 768,

  /** Уровни дробления гребня. Без них гора — гладкий колокол, то есть
   *  насыпь: у горного хребта силуэт изломан на всех масштабах сразу.
   *  Каждый уровень сажает на склоны предыдущего колокола помельче, а
   *  верхняя огибающая превращает их пересечения в рёбра.
   *
   *  Отрогов должно быть заметно больше, чем гор: на одну гору шириной
   *  0.3 радиана при сорока отрогах приходится два, при ста двадцати —
   *  шесть. Меньше двух — гора остаётся колоколом с одной зазубриной.
   *
   *  Подъём — насколько отрог вылезает над склоном под собой. Меньше
   *  десятой доли — ребро тонет в собственной толщине силуэта на таком
   *  удалении. Больше половины — отроги перерастают свои горы, и хребет
   *  превращается в частокол одинаковых пик.
   *
   *  Доля ширины строго меньше единицы: отроги шире шага между ними
   *  сливаются обратно в ровный вал, и всё дробление пропадает. */
  дробление: [
    { отрогов: 40, подъём: 0.3, доляШирины: 0.7 },
    { отрогов: 120, подъём: 0.16, доляШирины: 0.6 },
  ] as УровеньДробления[],

  /** Зерно раскладки отрогов. Как и всюду: без него горы пересобирались
   *  бы при каждой перезагрузке. */
  зерно: 20260826,

  /** Кольца от ближнего к дальнему. Три штуки: меньше — горизонт закрыт,
   *  но простор не читается, глазу не за что цепляться, отмеряя даль;
   *  больше — планы наплывают друг на друга и сливаются, потому что дымка
   *  на этих расстояниях уже почти уравнивает их тон.
   *
   *  Радиус обязан быть больше рельеф.размер / 2 = 200 — иначе кольцо
   *  окажется внутри равнины и воткнётся в снег.
   *
   *  ВЫСОТЫ РАСТУТ БЫСТРЕЕ РАДИУСА, и это главное число этого блока.
   *  Видимая высота — это высота, делённая на дальность. Сделай её
   *  одинаковой у всех колец — и дальние спрячутся за ближним ровно за
   *  своим силуэтом, три кольца сольются в одно. Здесь отношение растёт
   *  от 0.13 к 0.16, поэтому каждое следующее кольцо выглядывает
   *  из-за предыдущего.
   *
   *  Углы вершин у колец разные намеренно: совпади они, горы выстроились
   *  бы в затылок друг другу и планы перестали бы читаться раздельно.
   *
   *  Вершин по восемь на кольцо, и ширины у них РАЗНЫЕ — от 0.16 до 0.4
   *  радиана. Первый заход был из пяти одинаково широких колоколов, и
   *  горизонт вышел чередой пологих холмов: гора отличается от холма
   *  именно тем, что у неё есть острая вершина. Узкие пики вперемешку с
   *  широкими основаниями дают гребень, а не волну.
   *
   *  Цвет у гор СВОЙ, и это отступление от правила «дальнее красит
   *  дымка». Прежние гряды брали цвет снега — и были в кадре не видны
   *  совсем, что и записано в журнале этапа атмосферы как «читаются
   *  слабо». Причина считается на бумаге: цвет дымки #dfe4e8 почти равен
   *  цвету снега, а на четырёх сотнях метров дымка съедает три четверти
   *  разницы. Гора цвета снега в дымке цвета снега — это дымка.
   *
   *  Значит силуэт обязан быть темнее и холоднее фона ДО того, как его
   *  разбавит дымка. Здесь запас такой, что после разбавления на ближнем
   *  кольце остаётся около десятой доли контраста, на дальнем — около
   *  четырёх сотых: горы читаются планами, но не превращаются в тёмную
   *  стену вокруг светлой сцены.
   *
   *  Цветов ДВА, по высоте. Одноцветная стена читается вырезанной из
   *  бумаги наклейкой: у настоящей горы подножие тонет в приземной мгле и
   *  тем светлее, чем ниже, а гребень остаётся тёмным на фоне неба.
   *  Разница между ними невелика намеренно — это дымка, а не раскраска. */
  цветПодножия: '#c4ced8',
  цветВершины: '#8c9cad',
  кольца: [
    {
      радиус: 260,
      вершины: [
        { угол: 0.35, ширина: 0.3, высота: 34 },
        { угол: 1.05, ширина: 0.18, высота: 22 },
        { угол: 1.8, ширина: 0.35, высота: 30 },
        { угол: 2.55, ширина: 0.2, высота: 18 },
        { угол: 3.25, ширина: 0.32, высота: 33 },
        { угол: 4.0, ширина: 0.16, высота: 20 },
        { угол: 4.75, ширина: 0.28, высота: 27 },
        { угол: 5.6, ширина: 0.22, высота: 24 },
      ] as ВершинаКольца[],
    },
    {
      радиус: 330,
      вершины: [
        { угол: 0.7, ширина: 0.34, высота: 48 },
        { угол: 1.45, ширина: 0.2, высота: 30 },
        { угол: 2.2, ширина: 0.3, высота: 42 },
        { угол: 2.95, ширина: 0.18, высота: 26 },
        { угол: 3.7, ширина: 0.36, высота: 46 },
        { угол: 4.5, ширина: 0.22, высота: 33 },
        { угол: 5.25, ширина: 0.3, высота: 40 },
        { угол: 6.0, ширина: 0.18, высота: 28 },
      ] as ВершинаКольца[],
    },
    {
      радиус: 410,
      вершины: [
        { угол: 0.15, ширина: 0.38, высота: 66 },
        { угол: 0.95, ширина: 0.22, высота: 44 },
        { угол: 1.7, ширина: 0.34, высота: 58 },
        { угол: 2.45, ширина: 0.2, высота: 38 },
        { угол: 3.2, ширина: 0.4, высота: 64 },
        { угол: 4.05, ширина: 0.24, высота: 48 },
        { угол: 4.85, ширина: 0.32, высота: 56 },
        { угол: 5.7, ширина: 0.2, высота: 42 },
      ] as ВершинаКольца[],
    },
  ],
}

export const снег = {
  /** Сколько снежинок. Полторы тысячи — это ОДИН вызов отрисовки и ни
   *  одной координаты, посчитанной процессором в кадре: движение целиком
   *  живёт в шейдере. Меньше пятисот — воздух пустой, снег читается
   *  случайными точками грязи на объективе. Больше пяти тысяч — на
   *  светлом фоне это уже не воздух, а помеха: белые точки спорят с
   *  кладкой за внимание, а кладка здесь главная. */
  частиц: 1500,

  /** Сторона куба со снегом вокруг камеры, метры. Куб едет за камерой, и
   *  снежинка, вышедшая за грань, тем же остатком от деления появляется у
   *  противоположной — поэтому снег не кончается никогда и его ровно
   *  столько, сколько попадает в кадр. Шестьдесят метров: меньше тридцати
   *  — граница куба видна даже при затухании, дальше сотни — те же
   *  полторы тысячи снежинок размазываются по объёму втрое большему, и
   *  снегопад редеет до незаметности. */
  коробка: 60,

  /** Скорость падения, метры в секунду. Снег, а не дождь: 1.2 — это
   *  медленное оседание крупных хлопьев. Выше трёх читается ливнем и
   *  ломает покой сцены, ниже половины снежинки словно висят. */
  скорость: 1.2,

  /** Размер снежинки в ФИЗИЧЕСКИХ пикселях на опорной дальности. Именно
   *  физических, а не тех, которыми меряют вёрстку: на экране с двойной
   *  плотностью это вдвое меньше, чем кажется по числу. Первый заход был
   *  2.6 при опоре 10 — и снег не было видно вовсе: на дальнем краю куба
   *  снежинка выходила 0.9 физического пикселя, то есть меньше половины
   *  пикселя вёрстки. Точка тоньше пикселя не исчезает, но бледнеет
   *  ровно во столько же раз. */
  размер: 7,

  /** Опорная дальность для размера, метры. На ней снежинка занимает ровно
   *  `размер` пикселей, ближе — не растёт дальше. Ограничение нужно:
   *  снежинка, попавшая вплотную к камере, иначе раздувается в белое
   *  пятно на пол-экрана. Двадцать метров — это треть куба: снежинки на
   *  дальней грани получаются вдвое мельче ближних, и это единственное,
   *  что даёт снегопаду глубину. */
  опора: 20,

  /** Амплитуда бокового покачивания, метры, и его частота, 1/с. Без
   *  качания снег падает отвесными нитями и выглядит дождём: именно
   *  боковой снос отличает снежинку от капли. Больше метра — снежинки
   *  начинают заметно ездить поперёк, и глаз видит не ветер, а
   *  синусоиду. */
  качание: 0.35,
  частотаКачания: 0.6,

  /** С какой доли полукуба снежинки начинают гаснуть. Без затухания
   *  граница куба видна прямой линией, на которой снег обрывается.
   *  0.8 — компромисс: раньше снежинки начинали гаснуть уже в семнадцати
   *  метрах от камеры, то есть почти весь снегопад шёл полупрозрачным и
   *  в кадре его было не найти. Позже 0.9 — граница куба начинает
   *  выдавать себя резкой сменой плотности. */
  началоЗатухания: 0.8,

  /** Цвет снежинки. НЕ чисто белый, и это не вкусовщина: порог свечения в
   *  постобработке 0.95 (см. свечение.порог), а чисто белые точки прошли
   *  бы через bloom и дали по кадру мутную сыпь вокруг каждой снежинки.
   *  Здесь яркость ниже порога, и снег остаётся снегом. */
  цвет: '#f0f4f8',

  /** Непрозрачность снежинки в её середине. Снег на светлом фоне не
   *  может быть плотным: при единице точки читаются наклейками, вырезом
   *  в кадре. Три четверти — граница, за которой снежинка ещё воздух, но
   *  уже видна: на половине её на светлом небе просто нет. */
  плотность: 0.75,

  /** Доля радиуса точки, остающаяся плотной; дальше к краю прозрачность
   *  сходит на нет. Ноль дал бы размытое облачко без формы, единица —
   *  квадратный пиксельный край. */
  ядро: 0.3,

  /** Зерно раскладки снежинок. Как и у рельефа: без явного зерна снег
   *  пересобирался бы при каждой перезагрузке, и кадр перестал бы
   *  повторяться. */
  зерно: 20260826,
}

export const материалСнега = {
  /** Цвет снега. Светлее кирпича намеренно и это важнее всех остальных
   *  настроек: если снег окажется темнее объекта, объект растворится в
   *  фоне. Не чисто белый — чистый белый выбивается в пересвет и теряет
   *  всю светотень, по которой читается рельеф. */
  цвет: '#dfe6ec',

  /** Шероховатость. Высокая намеренно: снег почти не отражает. Ниже 0.6
   *  земля начинает бликовать, ловит свет изнутри сферы и выдаёт белое
   *  пятно под объектом — тот самый пересвет, который слоями не лечится. */
  шероховатость: 0.85,

  /** Металличность. Ноль: снег — диэлектрик. */
  металличность: 0,
}

export const небо = {
  /** Панорама. Формат HDR, а не WebP: нужен полный диапазон яркости,
   *  иначе панорама перестанет работать источником освещения. */

  /** РАЗРЕШЕНИЕ ПАНОРАМЫ — 1K, а не 2K, и это решение про вес отгрузки, а
   *  не про картинку. Файл HDR не сжимается как обычная картинка: 2K
   *  весил четыре мегабайта — больше, чем весь остальной код и все
   *  текстуры вместе. При этом небо у нас размыто (см. размытие ниже) и
   *  вдобавок служит источником рассеянного освещения, а освещению
   *  разрешение почти безразлично: оно всё равно усредняется по полусфере.
   *  Разницы в кадре не видно, минус три мегабайта на первой загрузке. */
  панорама: '/textures/sky.hdr',

  /** Яркость панорамы как фона. Отдельно от освещения намеренно: фон
   *  часто нужно притушить, чтобы небо не спорило со сферой, но при этом
   *  сохранить освещение от него. */
  яркостьФона: 1,

  /** Яркость окружающего освещения от панорамы. Складывается с солнцем
   *  и рассеянным, которые УЖЕ настроены и проверены. Выше единицы —
   *  снег уходит в белое и теряет светотень, по которой читается рельеф.
   *  Ноль — кирпичи отражают пустоту и выглядят вырезанными из кадра. */
  яркостьОсвещения: 0.35,

  /** Размытие фона. Небольшое намеренно: панорама на весь экран
   *  выдаёт себя резкими краями облаков, а лёгкое размытие превращает
   *  её в атмосферу. Больше 0.3 — небо становится однородной кашей. */
  размытие: 0.08,
}

export const дымка = {
  /** Цвет дымки. ОБЯЗАН совпадать с цветом неба у горизонта: объекты
   *  растворяются именно в этот цвет, и если он другой, дальние гряды
   *  повиснут силуэтами на фоне неба вместо того чтобы в него уйти. */
  цвет: '#dfe4e8',

  /** Плотность экспоненциального тумана, 1/метр. Подбирается ПО СФЕРЕ,
   *  а не по горам: дымка действует на всё, и при переборе главный
   *  объект бледнет вместе с фоном и перестаёт быть главным. При 0.004
   *  сфера в 30 метрах ещё контрастна, а гряда в 300 уже почти
   *  растворена — это и нужно. Ноль убирает глубину целиком. */
  плотность: 0.0028,
  // Подобрано на собранной версии по ГРЯДАМ, а не только по сфере.
  // При 0.004 ближний план съедался на две трети, дальний на 93% —
  // планы переставали различаться, и простор, ради которого они
  // ставятся, пропадал. Сферу снижение почти не задело: она в тридцати
  // метрах и теряет от дымки меньше процента при любом из значений.
  // Ставить ниже 0.002 нельзя — тогда дальняя гряда станет такой же
  // чёткой, как ближняя, и глубина схлопнется.
}

export const туман = {
  /** На сколько метров поверхность тумана поднята над землёй. Меньше
   *  метра — туман сливается со снегом и не читается вовсе. Больше
   *  четырёх — он поднимается выше дюн, и рельеф под ним пропадает. */
  надЗемлёй: 1.5,

  /** Плотность сетки поверхности тумана. Заметно грубее земли: у тумана
   *  нет ни бликов, ни резких краёв, и мелкие подробности рельефа в нём
   *  всё равно не видны, а вершин он стоит столько же. */
  сегментов: 96,

  /** Непрозрачность. Выше 0.5 туман превращается в молоко и съедает
   *  равнину целиком. Ноль — этапа не было. */
  непрозрачность: 0.28,

  /** Цвет. Тот же, что у дымки: два тумана в одном кадре разного цвета
   *  сразу читаются как ошибка. */
  цвет: '#dfe4e8',
}

export const лучКурсора = {
  /** Шаг марша, метры. Чуть больше ячейки сетки земли (400/384 ≈ 1.04 м):
   *  крупнее — риск перескочить резкий перепад дюны между двумя шагами и
   *  найти пересечение мимо настоящего; мельче — марш дороже без выигрыша
   *  в точности, которую всё равно скрывает пятно радиусом в три метра. */
  шаг: 1.5,

  /** Дальность марша, метры. Камера стартует на удалении около 29 метров
   *  от цели (см. сцена.камера.позиция) и на этом этапе остаётся обзорной
   *  — курсору незачем находить точку за пределами равнины, которую видно
   *  вокруг сферы. Пятикратный запас перекрывает разумное приближение и
   *  разворот OrbitControls. Больше — впустую тратим шаги марша на курсор,
   *  направленный в пустое небо за горизонтом. */
  дальность: 150,

  /** Число делений пополам при уточнении точки пересечения. Двенадцать
   *  дают точность порядка 1.5 / 2¹² ≈ 0.4 мм — запас на три порядка
   *  больше пятна радиусом в три метра. Меньше не проверялось: эти
   *  итерации стоят исчезающе мало по сравнению с самим маршем. */
  уточнений: 12,
}

export const следы = {
  /** Сторона холста в пикселях. При 400 метрах равнины это 0.78 метра
   *  на пиксель — соразмерно сетке земли, у которой ячейка чуть больше
   *  метра. Рисовать точнее, чем сетка умеет показать, бессмысленно:
   *  вершин под мелким пятном просто нет. */
  размерХолста: 512,

  /** Радиус пятна под курсором, метры. Меньше двух — вмятина попадает
   *  между вершинами сетки и не появляется вовсе. Больше шести — снег
   *  проминается корытом, и это читается не следом, а обвалом. */
  радиусПятна: 3,

  /** Яркость пятна, доля. Связана с прозрачностью заливки: если пятно
   *  ярче, чем гаснет, след не затянется никогда и равнина покроется
   *  коркой. Настраивать эти два числа только вместе. */
  яркостьПятна: 0.5,

  /** Прозрачность заливки фоном каждый кадр. Это и есть весь механизм
   *  затухания: старые пятна выцветают сами, без таймеров и списка
   *  точек. Больше 0.1 — след гаснет быстрее, чем зритель успевает его
   *  заметить. Ноль — не гаснет никогда. */
  затухание: 0.03,

  /** Сила затенения в следе. Карта смещения двигает вершины, но НЕ
   *  пересчитывает нормали — освещение остаётся прежним, и на однородном
   *  белом снегу ямка становится физически неразличимой: остаётся сдвиг
   *  силуэта в считанные пиксели. Проверено измерением: холст темнел
   *  правильно, а на экране не было ничего.
   *
   *  Поэтому тот же холст идёт вторым назначением — картой затенения.
   *  Белый фон не затеняет, тёмное пятно притемняет снег, и примятый
   *  снег читается как в жизни: темнее и глубже. Ноль возвращает
   *  невидимый след. Больше 1.5 — вместо вмятины чёрная клякса. */
  затенение: 1.0,

  /** Глубина вмятины, метры. Это масштаб карты смещения. Отрицательной
   *  быть не может: карта смещения двигает вершины ВДОЛЬ нормали, и
   *  знак задаётся не здесь, а тем, что пятно рисуется тёмным по
   *  светлому фону. Больше метра — вмятина глубже, чем сам наст. */
  глубина: 0.7,
}

export const текстуры = {
  земля: {
    /** Пути к картам от корня public. */
    цвет: '/textures/snow-color.webp',
    нормали: '/textures/snow-normal-gl.webp',
    шероховатость: '/textures/snow-roughness.webp',

    /** Сколько метров приходится на один повтор текстуры. Вторая вещь,
     *  которую проверяют при мыле после анизотропии: тайл в девять
     *  метров теряет детали при любом разрешении карты. Меньше двух —
     *  рисунок начинает повторяться заметной сеткой, и это видно на
     *  дальних планах. Компромисс, а не оптимум. */
    метраж: 3,
  },

  кирпич: {
    нормали: '/textures/ice-normal-gl.webp',

    /** Метраж для кирпича считается от его ширины, а не от стороны
     *  плоскости: кирпич мелкий, и повтор в три метра растянул бы одну
     *  текстуру на десяток кирпичей. */
    метраж: 0.5,

    /** Сила рельефа нормалей. Подобрана на крупном плане, а не на глаз
     *  с общего: при 0.35 рельефа не видно ВООБЩЕ — грани остаются
     *  стеклянными, и кажется, будто карта не подключилась. При 3.0
     *  поверхность становится мелко изъеденной и начинает читаться
     *  бетоном, а не льдом. Полтора — заметно, что кирпич не полирован,
     *  и при этом он всё ещё лёд.
     *
     *  Проверять это значение можно только вблизи: с обзорной камеры
     *  кирпич занимает три десятка пикселей, и любая сила выглядит
     *  одинаково. */
    сила: 1.5,
  },
}

export const прокрутка = {
  /** Высота блока со сценой в экранах.
   *
   *  ЭТО ГЛАВНЫЙ РЫЧАГ ПЛАВНОСТИ ПРИБЛИЖЕНИЯ, и вот почему. У прокрутки
   *  колесом нет промежуточных значений: щелчок даёт около ста пикселей,
   *  и сколько таких щелчков укладывается в приближение — столько разных
   *  положений камеры зритель и увидит. Шестьдесят восемь метров пути на
   *  четырёх экранах — это дюжина щелчков по пять метров каждый; на шести
   *  — два десятка по три с половиной. Догон (см. скоростьДогона)
   *  растягивает каждый щелчок во времени, но взять недостающие положения
   *  ему неоткуда, и длина пути остаётся первичной.
   *
   *  Шесть, а не больше: на восьми зритель начинает скучать между
   *  точками траектории — камере просто нечем занять лишний экран.
   *
   *  Изменение этого числа МЕНЯЕТ порог нахлёста (см. порогНахлёста), но
   *  НЕ меняет длину разгорания: она задана в экранах и отсчитывается от
   *  порога назад, так что весь добавленный путь достаётся приближению,
   *  ради которого блок и растянули. */
  экранов: 6,

  /** Насколько лендинг наезжает на хвост сцены, в экранах. Без нахлёста
   *  зритель, уже отсмотревший переход, ещё какое-то время пялится в
   *  кадр, залитый ровным цветом. Больше двух — страница закроет кадр
   *  раньше, чем траектория доиграна. */
  нахлёстВЭкранах: 1,

  /** Сколько экранов прокрутки отдано разгоранию — тому отрезку, где
   *  камера уже встала у проёма, а свет из Б забирает кадр.
   *
   *  В ЭКРАНАХ, А НЕ ДОЛЕЙ ОТ ВСЕЙ ПРОКРУТКИ, и это не мелочь. Долей оно
   *  и было, пока блок не растянули с четырёх экранов до шести ради
   *  плавности: разгорание тогда растянулось бы втрое, хотя его
   *  подбирали глазами и трогать не собирались. В экранах длина держится
   *  сама, на сколько бы экранов ни растянули блок.
   *
   *  Полэкрана. На четырёх экранах при нахлёсте в один это давало конец
   *  сцены ровно на 0.5 — ту самую долю, что стояла здесь числом до
   *  формулы, и это способ проверить, что формула описывает уже принятую
   *  картинку, а не новую. Меньше четверти экрана — свет вспыхивает
   *  разом, переход читается сбоем. Больше экрана — камера давно стоит, а
   *  зритель всё крутит. */
  разгораниеВЭкранах: 0.5,

  /** Скорость догона прокрутки, 1/с, в показателе экспоненты.
   *
   *  Нужна потому, что у прокрутки колесом нет промежуточных значений.
   *  Вся траектория — шестьдесят восемь метров — укладывается примерно в
   *  тысячу двести пикселей, а щелчок колеса даёт около ста: на всё
   *  приближение приходится дюжина положений, и камера прыгает по пять
   *  метров за раз. Догон растягивает каждый прыжок на несколько кадров
   *  (см. догнатьПрогресс).
   *
   *  Семь — это постоянная времени около одной седьмой секунды: скачок
   *  превращается в проезд длиной чуть меньше полусекунды. Работает в
   *  паре с длиной пути (см. экранов): на шести экранах щелчок стоит
   *  три с половиной метра вместо пяти, и отставание в метрах при том же
   *  сглаживании выходит МЕНЬШЕ прежнего, хотя само сглаживание сильнее.
   *
   *  Меньше пяти — сцена заметно тащится за полосой прокрутки, и лендинг
   *  приезжает на кадр, пока камера ещё летит. Больше двадцати — скачок
   *  размазывается на два-три кадра, а этого не хватает, чтобы ступенька
   *  перестала читаться. */
  скоростьДогона: 7,

}

export const траектория = {
  /** Путь камеры вдоль прокрутки: две кривые Катмулла-Рома по четыре
   *  точки каждая — одна ведёт положение камеры, другая независимо ведёт
   *  точку, куда камера смотрит.
   *
   *  Кривая открытая (не closed), поэтому getPointAt(0) — это РОВНО
   *  первая точка, а не что-то около неё. Отсюда требование: первая
   *  точка позиции совпадает с сцена.камера.позиция. При прогрессе 0 кадр
   *  обязан остаться тем же, что показывает страница до прокрутки.
   *
   *  БЕЗ ВИЛЯНИЯ. Азимут опорных точек (угол вокруг вертикальной оси
   *  сферы) идёт ТОЛЬКО в одну сторону: 33.7°, 33.7°, −39.3°, −55.1°.
   *  Раньше последняя точка отыгрывала назад (−39.3° → −26.6°), и обход
   *  читался как «сфера уехала влево, а в конце вернулась вправо» —
   *  зритель видел не облёт, а рывок оператора. То же было и с целями:
   *  они виляли по X (0, −1, +1, −2.7), и сфера гуляла по кадру взад-вперёд.
   *
   *  Теперь ЦЕЛИ СТОЯТ НА ОСИ СФЕРЫ и отличаются только высотой. Смысл:
   *  горизонтальное положение сферы в кадре задаётся целью, и любое её
   *  боковое смещение — это гуляние сферы по экрану. Ощущение живого
   *  облёта даёт САМ ПУТЬ камеры, а не подёргивание взгляда. Проверено
   *  расчётом по 400 точкам дуги: сфера удерживается в центре кадра с
   *  точностью до 1e-4 по горизонтали, разворотов движения нет НИ ОДНОГО
   *  ни по горизонтали, ни по вертикали.
   *
   *  Небольшой ПОДЪЁМ цели (14 → 14.7) оставлен намеренно: сфера за путь
   *  плавно оседает в кадре на 19% высоты, кадр перестаёт быть мёртво
   *  симметричным. Подъём монотонный, поэтому виляния он не даёт.
   *
   *  ЗАПАС ПО ВЫСОТЕ. Путь посчитан численно, а не прикинут на глаз:
   *  длина 66.8 м, самая низкая точка ПОЗИЦИИ — 6.00 м (это сама первая
   *  опорная точка, значит между точками кривая НЕ провисает ниже них),
   *  наименьшая дальность до центра сферы — 8.96 м при радиусе 6, то есть
   *  камера подходит к кладке на 2.96 м и внутрь не заходит.
   *
   *  Под первой точкой земля не голая: она попадает в дюну (24, 78) с
   *  радиусом 38 м — расстояние до её центра 31 м, колокол даёт там
   *  0.27 м, плюс наст с амплитудой 0.45 м. Земля под камерой около
   *  0.7 м, камера идёт на шести — запас больше пяти метров. Остальные
   *  три дюны дальше своих радиусов, дальние гряды на высоту не влияют.
   *
   *  ОТКРЫТЫЙ КРАЙ РАВНИНЫ. Дальние гряды (гряды.планы) стоят только
   *  вдоль ОТРИЦАТЕЛЬНОЙ оси Z, на 260-410 метрах. С остальных сторон
   *  равнина за 200 метрами просто кончается — камере нельзя туда
   *  смотреть. Поэтому Z у всех точек положительный (минимум по всему
   *  пути 5.10), камера всегда «перед» сферой, если считать от гряд, а
   *  взгляд направлен на сферу и продолжается за неё в сторону минус Z —
   *  то есть в гряды, а не в пустоту.
   *
   *  Числа проверены расчётом, но КАРТИНКУ всё равно надо смотреть
   *  глазами: «камера не задевает дюну» формализуется, а «кадр не
   *  разваливается» — нет. */
  позиции: [
    // 1. Общий план. Совпадает с сцена.камера.позиция — первый кадр
    //    страницы не меняется. Дальность до центра сферы ≈58 м: сфера
    //    маленькая на фоне простора, читается масштаб равнины.
    //    Первый отрезок пути длиннее остальных (58 м до 18 м), и это
    //    намеренно: разгон по длине дуги отдаёт ему больше прокрутки,
    //    зритель успевает разглядеть простор до того, как начнётся обход.
    [32, 6, 48],
    // 2. Приближение. Дальность падает до ≈18 м: сфера заметно крупнее,
    //    начинает читаться сама кладка, а не только силуэт шара.
    //    Азимут тот же, что у первой точки, — пока это чистый наезд без
    //    поворота, чтобы приближение не смешивалось с обходом.
    [16, 11, 24],
    // 3. Обход. Камера проходит по дуге в 73° вокруг сферы (33.7° →
    //    −39.3°), по-прежнему держась положительного Z. Дальность ≈14 м:
    //    виден уже другой бок сферы, дальние гряды остаются задником.
    [-4.16, 12.5, 23.59],
    // 4. У ВХОДА В ПРОЁМ, в 12.5 м от центра A. Обход продолжается в
    //    ту же сторону (−10° → −55°), а не отыгрывает назад.
    //
    //    ПРИБЛИЖЕНИЕ ЗДЕСЬ И ЗАКАНЧИВАЕТСЯ. Раскрытая кладка занимает
    //    кадр целиком: проём радиусом 3.1 м с шести метров виден под
    //    углом 25.5° при половине поля зрения 22.5°. Дальше прокрутка
    //    отдана свету — шарик Б разгорается и раздувается, см.
    //    свет.шарик.радиусРазгоревшийся.
    //
    //    Внутрь камера больше НЕ ЗАЛЕТАЕТ. Пробовал: внутри кадр
    //    становится плоской стеной кладки, а движение до самого центра
    //    съедало прокрутку, которая нужна свету. Накрывает зрителя теперь
    //    не камера, а растущий свет.
    [-10.22, 14.66, 7.16],
  ] as [number, number, number][],

  цели: [
    // Все четыре точки стоят на вертикальной оси сферы: X и Z нулевые,
    // меняется только высота. Любое боковое смещение цели — это боковое
    // смещение сферы в кадре, а оно и давало виляние.
    //
    // Камера смотрит В СЕРЕДИНУ сферы и туда же летит. Пробовал задирать
    // взгляд на 46°, чтобы вывести из кадра самосветящийся шарик, — на
    // картинке это читается как «камера прошла выше входа», а не как
    // влёт внутрь. Шарик вместо этого уменьшен (см. свет.шарик.радиус).
    //
    // Подъём 14 → 14.4 монотонный и крошечный: в конце это 5° над
    // направлением на центр, шарик садится чуть ниже середины кадра и
    // кадр перестаёт быть мёртво симметричным. Разворотов движения нет
    // ни одного — ровно то, из-за чего цели вообще переехали на ось.
    //
    // Точки не делаются ОДИНАКОВЫМИ намеренно: у кривой из совпадающих
    // точек нулевая длина дуги, и getPointAt начинает делить на ноль.
    [0, 14, 0],
    [0, 14.15, 0],
    [0, 14.3, 0],
    [0, 14.4, 0],
  ] as [number, number, number][],
}

export const загрузка = {
  /** Сколько миллисекунд экран ожидания уезжает, растворяясь.
   *
   *  Не ноль: мгновенная подмена пустого прямоугольника готовым кадром
   *  читается как сбой, глаз не успевает понять, что произошло. Больше
   *  секунды — зритель уже видит сцену сквозь полупрозрачную заслонку и
   *  ждёт непонятно чего. Этим же числом задерживается снятие слоя: он
   *  убирается ПОСЛЕ того, как прозрачность дойдёт до нуля. */
  затухание: 450,
}

export const отладка = {
  /** Показывать оси базиса кирпичей. Выключены после успешной проверки
   *  кладки, а не удалены: оси были единственным способом увидеть
   *  пьяную кладку до того, как её замаскирует фаска, и понадобятся
   *  снова, когда сфера поедет по траектории камеры в следующей ветке.
   *  До тех пор они только мешают судить о картинке. */
  осиВидны: false,

  /** Оси рисуются не у всех кирпичей, а у каждого N-го: полтысячи троек
   *  отрезков нечитаемы и тормозят. */
  шагВыборки: 7,

  /** Длина отладочной оси, метры. Соразмерна высоте кирпича: длиннее —
   *  оси соседей перепутываются между собой. */
  длинаОси: 0.5,

  /** Толщина линии в пикселях. */
  толщинаЛинии: 1.5,
}

export const раскрытие = {
  /** Доля прокрутки, на которой створки ТРОГАЮТСЯ. К этому моменту
   *  камера ещё далеко (радиус 12 м она проходит только на 0.870), и
   *  сфера видна целиком — раскрытие читается как событие, а не как
   *  случайность у самого носа. Раньше 0.5 — створки расходятся, когда
   *  кладку ещё толком не разглядели, и весь предыдущий облёт
   *  обесценивается. */
  начало: 0.58,

  /** Доля прокрутки, на которой створки распахнуты ПОЛНОСТЬЮ. Оболочку
   *  камера пересекает на 0.959 (посчитано по кривой), так что 0.93
   *  оставляет запас: к моменту влёта в проёме уже нет кирпичей. Ставить
   *  ближе к 0.959 нельзя — сглаженный переход у самого конца идёт
   *  медленно, и последние сантиметры створка доедет уже под камерой. */
  конец: 0.90,

  /** Угловой радиус ЗОНЫ, метры не при чём — радианы по поверхности
   *  сферы. Кирпичи внутри этого угла от оси проёма участвуют в
   *  раскрытии, остальные стоят намертво.
   *
   *  0.95 рад — это 54°, доля затронутых кирпичей равна (1 − cos α)/2 =
   *  0.21, то есть около 107 кирпичей из 506. Меньше 0.6 — зона у́же
   *  будущего проёма, и раскрываться нечему: кирпичам некуда отъезжать.
   *  Больше 1.3 — в движение приходит половина сферы, и снова получается
   *  «шар развалился», а не «в шаре открылся вход». */
  угловойРадиусЗоны: 0.95,

  /** Угловой радиус самого ПРОЁМА, радианы. Кирпич у оси уезжает ровно
   *  на этот угол, и внутри него не остаётся никого.
   *
   *  0.55 рад — это 31.5°, дыра радиусом radius·sin(31.5°) = 3.1 м,
   *  диаметром 6.3 м. С шести метров она видна под углом 55° при поле
   *  зрения 45° — камера входит, не задевая краёв кадра.
   *
   *  ОБЯЗАН быть меньше угловогоРадиусаЗоны, иначе отображение углов
   *  перестаёт возрастать: кирпичи начинают перескакивать друг через
   *  друга, и кольца выворачиваются наизнанку. Чем ближе к зоне, тем
   *  сильнее сгущение у края проёма — при 0.55 против 0.95 шаг между
   *  кольцами сжимается в 0.42 раза, и это уже требует подъёма. */
  угловойРадиусПроёма: 0.55,

  /** Насколько кирпич у самой оси всплывает над оболочкой, метры.
   *
   *  Не украшение, а необходимость. Отъезжая к краю проёма, кирпичи
   *  сгущаются по меридиану: шаг между кольцами 0.673 м сжимается до
   *  0.28 м, а высота кирпича — те же 0.673 м. Без подъёма они оказались
   *  бы внутри друг друга. Подъём разводит их по радиусу, и вокруг
   *  проёма вырастает воронка из приподнятой кладки.
   *
   *  Три метра — не на глаз, а из шага колец. Кольца отстоят друг от
   *  друга на 0.673/6 = 0.112 рад, вес по зоне падает линейно, значит
   *  соседние кольца всплывают на разную высоту: 3 · 0.112/0.95 = 0.354 м.
   *  Это чуть больше толщины кирпича 0.35 — кладка ложится внахлёст, как
   *  чешуя, и кирпич в кирпич не входит. Проверено на настоящей раскладке
   *  в tests/opening.test.ts: наименьшее расстояние между центрами 0.47 м
   *  при подъёме 3 и 0.28 м без подъёма.
   *
   *  Меньше 2.5 — соседние кольца сходятся ближе толщины и срастаются.
   *  Больше 4.5 — воронка превращается в торчащий венец и перестаёт
   *  читаться как часть сферы. */
  подъём: 3,
}

export const свечение = {
  /** Порог яркости, выше которого пиксель уходит в ореол. Это главное
   *  число всей постобработки.
   *
   *  Снег на солнце — около 0.8, самосветящийся шарик — 3 и выше
   *  (см. свет.шарик.яркость, тональная компрессия для него выключена).
   *  Порог обязан лежать между ними. При 0.8 и ниже в ореол уходит вся
   *  равнина, и снег превращается в молочную кашу — это самая заметная
   *  ошибка, которую здесь можно сделать. Выше 1.3 не загорается уже и
   *  свет в щелях между кирпичами, ради которого всё затевалось. */
  порог: 0.95,

  /** Ширина размытия порога. Ноль даёт жёсткую границу свечения: на
   *  склоне дюны, где яркость плавно подходит к порогу, появляется
   *  видимый контур — светится половина склона, вторая нет. */
  мягкость: 0.2,

  /** Сила ореола. Ниже 0.6 шарик остаётся диском, ради чего всё и
   *  делалось. Выше 2 ореол забивает кладку вокруг проёма, и внутри
   *  сферы не остаётся ничего, кроме света. */
  сила: 1.2,

  /** Радиус размытия ореола, доля кадра. Больше 0.85 — ореол размазан по
   *  всему экрану и читается как грязное стекло, а не как свет. */
  радиус: 0.6,
}

export const вспышка = {
  /** Момента начала вспышки здесь НЕТ, и это намеренно. Он ищется по
   *  самой траектории: доля пути, на которой камера входит внутрь сферы
   *  A, переведённая во время прокрутки. Свет из Б обязан пойти ровно
   *  тогда, когда зритель оказывается внутри.
   *
   *  Пока это было числом, оно разъезжалось со всем подряд — это третий
   *  случай подряд. Правило: если два момента обязаны совпадать, они
   *  обязаны быть ОДНОЙ формулой, а не двумя настройками рядом. */

  /** Сколько точек перебрать по длине пути, разыскивая вход внутрь сферы.
   *  Пересечение кривой Катмулла-Рома со сферой аналитически не решается,
   *  остаётся перебор. Тысяча даёт погрешность в тысячную долю пути —
   *  меньше пикселя прокрутки. Считается ОДИН раз при монтировании, не в
   *  кадре, так что дешевизна тут не важна; важнее не промахнуться мимо
   *  тонкого касания оболочки. */
  шаговПоиска: 1000,

  /** С какой доли РАЗГОРАНИЯ кадр начинает доводиться до цвета подложки.
   *
   *  Основную работу делает сам шарик: он раздувается, и его ореол
   *  съедает кладку. Но белый ореол — это #ffffff, а лендинг лежит на
   *  сцена.фон, и на стыке была бы видна светлая полоса. Поэтому в
   *  последней трети разгорания поверх добавляется мягкая доводка до
   *  цвета страницы.
   *
   *  0.65 — доводка включается, когда кадр уже почти белый, и её самой не
   *  видно. Ниже 0.4 она гасит кадр раньше, чем шарик успел разгореться,
   *  и переход читается затуханием, а не вспышкой. */
  порогЗаливки: 0.65,

  /** Момента ОКОНЧАНИЯ разгорания здесь тоже нет: он равен порогу
   *  нахлёста, то есть приходу лендинга на кадр. Считается функцией
   *  конецРазгорания.
   *
   *  Раньше тут стояло 0.88, и свет тянулся почти до низа страницы, хотя
   *  смотреть там уже было не на что — кадр наполовину закрыт заглушкой.
   *  Теперь окно разгорания — ровно промежуток между концом сцены и
   *  приходом страницы, и другого места ему нет. Задаётся оно с другого
   *  конца: длиной в экранах (прокрутка.разгораниеВЭкранах), от которой
   *  конец сцены и отсчитывается назад. */

  /** До какой доли ширины экрана дорастает фронт вспышки. Больше
   *  единицы намеренно: угол кадра дальше от центра, чем край, и при
   *  ровно единице углы остаются нетронутыми, когда всё остальное уже
   *  залито. Полтора хватает при любом положении центра. */
  дальность: 1.5,

  /** Ширина размытия фронта, доля экрана. Ноль даёт режущий край круга,
   *  как у ножниц. Больше 0.4 — фронт перестаёт читаться как фронт, и
   *  вспышка выглядит равномерным осветлением. */
  мягкость: 0.18,
}
