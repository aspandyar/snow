/** ЗАПИСАННЫЕ ЗАМЕРЫ. Единственные числа на странице, которые НЕ
 *  считаются на месте — и потому единственные, которые могут ОТСТАТЬ от
 *  сцены. Так и вышло после ветки фона: тень ушла, горы и снег пришли, а
 *  цифры на странице ещё месяц утверждали бы прежнее. Правило простое:
 *  меняешь состав сцены — перегоняешь замер.
 *
 *  Всё остальное на лендинге рисуется теми же функциями, что считают
 *  сцену, и врать не может по построению. С производительностью так не
 *  выйдет: её нельзя вывести из кода, её можно только измерить — и
 *  измерить на конкретной машине, в конкретном окне, конкретной сборкой.
 *
 *  Поэтому здесь лежат данные, а рядом с ними — условия, при которых они
 *  получены. Число без условий бессмысленно: «семь миллисекунд» ничего не
 *  значит, пока не сказано, на чём и в каком окне.
 *
 *  Как обновить: открыть собранную версию с `?measure`, дождаться отчёта
 *  в консоли и перенести его сюда целиком. Режим замера живёт в
 *  components/scene/Measure.tsx, там же написано, что именно он меряет. */

export type ЗамерТочки = {
  /** Ключ для перевода: сами подписи лежат в texts.ts, чтобы данные не
   *  зависели от языка. */
  ключ: 'общий' | 'облёт' | 'раскрытие' | 'разгорание'
  доля: number
  /** Медиана времени кадра, миллисекунды. */
  медиана: number
  /** Девяносто пятый процентиль — тот самый рывок, которого медиана не
   *  показывает. */
  хвост: number
  кадров: number
}

export const ЗАМЕРЫ = {
  снято: '2026-08-28',
  видеокарта: 'Apple M1 Pro (ANGLE Metal)',
  /** Размеры окна в независимых пикселях и плотность экрана. Числами, а
   *  не фразой: фраза зависит от языка страницы, и в английском режиме
   *  условия замера остались бы по-русски. Формулировки лежат в texts.ts,
   *  здесь только данные. */
  окно: { ширина: 934, высота: 994, плотность: 2 },

  /** ОКНО ЗДЕСЬ ВЕРТИКАЛЬНОЕ, и это важно с тех пор, как поле зрения
   *  подгоняется под форму окна: 934×994 у́же опорного отношения, значит
   *  замер снят с расширенным полем зрения — так же, как его увидит
   *  зритель с телефона, и не так, как зритель за широким монитором.
   *
   *  Окно крупнее прежнего (было 697×863), и сравнивать эти цифры с
   *  прошлыми в лоб нельзя: пикселей в кадре в полтора раза больше, а
   *  главный рычаг здесь именно пиксели. Ровно поэтому условия и лежат
   *  рядом с числами. */

  /** Одинаковы во всех точках: кладка рисуется одним вызовом на все
   *  восемьсот двадцать восемь кирпичей, а остальное — земля, небо,
   *  туман, три кольца гор, снег и проходы постобработки.
   *
   *  ТРЕУГОЛЬНИКОВ СТАЛО НА СОРОК ПРОЦЕНТОВ БОЛЬШЕ, чем в прошлом замере
   *  (было 972 493). Это плата за плотность кладки: колец стало 36 вместо
   *  28, кирпичей 828 вместо 506. Вызовов по-прежнему 26 — кладка как
   *  рисовалась одним вызовом, так и рисуется, сколько бы кирпичей в ней
   *  ни было. */
  вызововОтрисовки: 26,
  треугольников: 1388517,
  геометрий: 21,
  текстур: 26,
  программ: 13,

  /** Доли прокрутки здесь НЕ выписаны от руки: режим замера считает их
   *  теми же функциями, что задают эти моменты сцене (см. Measure.tsx).
   *  Оттого они и «некруглые» — 0.63 это конец раскрытия, переведённый из
   *  отсчёта сцены в отсчёт прокрутки. */
  точки: [
    { ключ: 'общий', доля: 0, медиана: 7.4, хвост: 8, кадров: 135 },
    { ключ: 'облёт', доля: 0.35, медиана: 8.1, хвост: 8.6, кадров: 123 },
    { ключ: 'раскрытие', доля: 0.63, медиана: 8.7, хвост: 9.2, кадров: 115 },
    { ключ: 'разгорание', доля: 0.75, медиана: 9.7, хвост: 10.1, кадров: 103 },
  ] as ЗамерТочки[],
}
